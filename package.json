{
  "name": "blockvale",
  "version": "1.0.0",
  "private": true,
  "description": "A Minecraft-style voxel sandbox for the browser with shader-pack graphics (WebGL2).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node tools/serve.mjs",
    "test": "node tools/smoke-test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
